#include<stdio.h>
void main(){
    int num = 125, num1 = 112;
    printf("the addition is%d", num + num1);
   // printf("\nthe substract is%d", num - num1);        //use \n to add code in new line 
    printf("\nthe multiplication is%d", num * num1);
    printf("\nthe division is%d", num / num1);

}

